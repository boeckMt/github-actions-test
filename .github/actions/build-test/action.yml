name: 'Build and Test'
description: 'Do a npm build and teat'

runs:
  using: "composite"
  steps:
    - uses: actions/setup-node@v2
      with:
          node-version: 14
    - run: npm ci
    - run: npm run test -- --headless
      shell: bash
    - run: npm run build
      shell: bash
